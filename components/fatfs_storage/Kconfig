menu "FATFS Settings"
    # This option controls whether the feature is enabled
    config ENABLE_NVS_SETTINGS
        bool "Enable FATFS Settings"
        default y
        help
          Enable this option to include SSID & Password in the build.

endmenu