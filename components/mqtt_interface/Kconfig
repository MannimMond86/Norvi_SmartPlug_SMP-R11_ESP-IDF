menu "MQTT Settings"

    config IS_ACTIVATED_MQTT
        bool "Enable MQTT Settings"
        default y

    if IS_ACTIVATED_MQTT

    config MQTT_PROJECT_VERSION
        prompt "Project Version"
        string
        default "1.0.0"
        help
            Specify the version of the ADE9153 project.

    endif # IS_ACTIVATED_MQTT

endmenu